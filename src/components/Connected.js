function Connected(){
// Message that will be used to tell if a person is already connected when he tries to go to pages that appear only
    // When the person has not logged in
    return (
        <div className="beConnected">
        <h2>Vous êtes déjà connectés</h2>
        </div>    )
}

export default Connected