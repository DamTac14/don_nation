import '../style/beConnected.css'

function BeConnected(){
    // Message that tells the user who is trying to access pages whose connection is requested
    return (
        <div className="beConnected">
        <h2>Vous devez être connecté pour accéder à cette page</h2>
        </div>
    )
}

export default BeConnected